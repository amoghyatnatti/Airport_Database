@model Airport_Database.Models.ExpertiseIn

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>ExpertiseIn</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Ssn" class="control-label">Technician</label>
                <select asp-for="Ssn" class ="form-control" asp-items="ViewBag.Ssn"></select>
            </div>
            <div class="form-group">
                <label asp-for="ModelNo" class="control-label"></label>
                <select asp-for="ModelNo" class ="form-control" asp-items="ViewBag.ModelNo"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
